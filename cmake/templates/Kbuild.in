#take external source dir if present
ifdef SRCDIR
src := $(SRCDIR)
else
src := $(obj)
endif

#module definition
obj-m     := @KMOD_NAME@.o
ccflags-y := @KMOD_INCLUDES@
@KMOD_NAME@-y := @KMOD_OBJECTS@
